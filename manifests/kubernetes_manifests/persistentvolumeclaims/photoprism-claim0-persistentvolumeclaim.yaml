apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: photoprism-claim0
  name: photoprism-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
