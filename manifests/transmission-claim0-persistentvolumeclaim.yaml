apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: transmission-claim0
  name: transmission-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
